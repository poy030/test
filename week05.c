#include<stdio.h>
int main()
{
    // int a = 100; //ฐาน2
    // int b = 0144; //ฐาน8
    // int c = 0x64; //ฐาน16
    
    // printf("%d %d %d", a, b, c);
    // int x = 100;
    // printf("value of x is %d\n", x);
    // printf("reference of x is %lu\n", &x);
    // printf("reference of x is hexadecimal is %p\n", &x);

    // int data[4] = {7, 8, 9, 10}; //ประกาศ arry
    // printf("value of data[0] is %d\n", data[]); // []สามารถเข้าถึงตัวอื่นได้แต่จะได้ 0
    // printf("reference of data[0] is %p\n", &data[]); //%p = reference
    // printf("reference of data is %p\n", data);

    // int x = 100;
    // int *ptr = &x;
    // // int *arrPtr = data;
    // // int *ptr;
    // // ptr = &x;
    // printf("x is %d\n", x);
    // printf(" reference of x is %p\n", &x); //%p = reference
    // printf("ptr is %p\n", ptr);
    // printf("dereferencing prt %d\n", *ptr);

    // int x = 100;
    // int *ptr = &x;
    // int *arrPtr = data;
    // int y = 1000;

    // printf("arrPtr = %p\n", arrPtr);
    // printf("*arrPtr = %d\n", *arrPtr);
    // x += 1;
    // // x = 101

    // arrPtr += 100;
    // printf("arrptr = %p\n", arrPtr);
    // printf("*arrPtr = %d\n", *arrPtr);

    // *arrPtr = 100;
    // printf("arrptr = %p\n", arrPtr);
    // printf("*arrPtr = %d\n", *arrPtr);

    // arrPtr++;
    // printf("arrptr = %p\n", arrPtr);
    // printf("*arrPtr = %d\n", *arrPtr);

    // int data[4] = {7, 8, 9, 10};

    // for (int *p = data; p < &data[4];p++){
    //     printf("[%p] = %d\n", p, *p);
    // }
    // for(int i = 0; i < 4; i++){
    //     printf("data[%d] = %d\n", i, data[i]);
    // }

    // int i;
     char s[10];
    // char c;
    // scanf("%d ", &i);
    // scanf("%c", &c);
    // printf("%c - %d\n", c, i);

    // scanf("%s", s);
    //  //*p != '\0'หยุดเมื่อเจอ noun character
    // for(int *p = s; *p != '\0'; p++){
    //     printf("[%p] = %c\n", p, *p);
    // }



}